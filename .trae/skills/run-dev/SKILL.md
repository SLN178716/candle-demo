---
name: "run-dev"
description: "Starts the frontend development server using pnpm. Invoke when the user wants to preview the application or start development."
---

# Run Dev Server

This skill starts the frontend development server.

## Usage

1.  Ensure you are in the project root.
2.  Run the following command:
    ```bash
    cd frontend && pnpm dev
    ```
